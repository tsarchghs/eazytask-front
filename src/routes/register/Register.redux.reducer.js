// import { POST_USER } from "./Register.redux.actionTypes";

// export const userReducer = (state = {  }, action) => {
//     if (action.error) return { ...action.error.response.data, type: action.type }
//     switch (action.type) {
//         case POST_USER:
//             return { ...action.payload, type: action.type }
//         default: return state;
//     }
// }
