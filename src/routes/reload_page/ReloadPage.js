import React from "react";

const ReloadPage = props => {
    return (
        <div>
            We could not load this page due to network issues. Please make sure you have good internet connection and reload the page.
        </div>
    )
}

export default ReloadPage;